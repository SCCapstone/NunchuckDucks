import { TouchableOpacity, ext, Image, View } from "react-native";
import React from "react";

const HomeHeader = ({ navigation }) => {
  return (
    <View style={{ width: "100%", height: 150 }}>
      <Image></Image>
    </View>
  );
};

export default HomeHeader;
